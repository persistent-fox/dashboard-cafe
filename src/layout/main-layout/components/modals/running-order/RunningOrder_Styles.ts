import styled from 'styled-components';

const RunningOrder = styled.div`
	position: absolute;
	width: 70%;
	right: 0;
	background-color: ${props => props.theme.colors.primary};
`;

export const S = { RunningOrder };
